# Auto-generado por Ansible — rol: factory_ui

DB_HOST={{ platform_endpoints.postgres_host }}
DB_PORT={{ platform_endpoints.postgres_port }}
DB_NAME={{ postgres_db }}
DB_USER={{ postgres_user }}
DB_PASS={{ postgres_password }}

NATS_URL={{ nats_url | default(platform_endpoints.nats_url) }}

STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0