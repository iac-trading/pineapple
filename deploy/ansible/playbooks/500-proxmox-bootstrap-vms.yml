# =================================================================
# 500 | Proxmox & Controller Bootstrap (Optimized Resources V3.1)
# =================================================================
---
- name: 500.1 | Controller | Preparar Máquina Controladora (Localhost)
  hosts: localhost
  connection: local
  become: true
  gather_facts: false
  tags: [ "bootstrap", "controller" ]

  tasks:
    - name: "Controller | Instalar dependencias del sistema para Postgres"
      ansible.builtin.apt:
        name:
          - python3-pip
          - libpq-dev
          - python3-dev
        state: present
        update_cache: yes

    - name: "Controller | Instalar librerías de Python necesarias para Ansible"
      ansible.builtin.pip:
        name:
          - psycopg2-binary
          - docker
          - pandas
        state: present

- name: 500.2 | Proxmox | Crear/Actualizar VMs Consolidadas (V3.0)
  hosts: localhost
  gather_facts: false
  tags: [ "proxmox", "provision" ]

  vars:
    # IP de tu nodo Proxmox físico
    pve_ssh_host: "10.100.100.1"
    # ID de tu VM base/template (Ubuntu/Debian con Cloud-Init)
    template_vmid: 9000
    storage_system: "local-trading"
    net_bridge: "vmbr0"
    vlan_gateway: "10.100.100.1"
    dns_servers: ["10.100.100.1", "1.1.1.1"]
    ssh_wait_timeout: 900

    # CONFIGURACIÓN DE ALTO RENDIMIENTO (OPTIMIZADA A 300GB RAM Y 3.8TB STORAGE MÁXIMOS)
    vms:
      # BRAIN: Orquestación, NATS, Airflow, UI, Grafana (Sin IA/Ollama)
      - { name: "VM-AQ-BRAIN", vmid: 801, cores: 8, mem: 16384,  root_gb: 150,  ip: "10.100.100.201/24" }
      
      # DATA: TimescaleDB (Optimizado para aprovechar 96GB RAM)
      - { name: "VM-AQ-DATA", vmid: 802, cores: 16, mem: 98304,  root_gb: 1500, ip: "10.100.100.202/24" }
      
      # COMPUTE: Agente y Estrategias LIVE
      - { name: "VM-AQ-COMPUTE", vmid: 803, cores: 12, mem: 49152,  root_gb: 300,  ip: "10.100.100.203/24" }
      
      # IA: Ollama y Estrategias IA (Se separa la IA a su propia VM con 64GB de RAM)
      - { name: "VM-AQ-IA", vmid: 804, cores: 16, mem: 65536,  root_gb: 500,  ip: "10.100.100.204/24" }

      # TRADE: Forex, IBKR Gateway y otros proveedores (Reemplaza a LAB)
      - { name: "VM-AQ-TRADE", vmid: 805, cores: 12, mem: 65536,  root_gb: 300,  ip: "10.100.100.205/24" }

  pre_tasks:
    - name: Preparar DNS string
      set_fact:
        dns_string: "{{ dns_servers | join(' ') }}"

  tasks:
    - name: Eliminar VM previa si existe (Limpieza de Nodos 801-805)
      delegate_to: "{{ pve_ssh_host }}"
      become: true
      shell: |
        set -e
        VMID="{{ item.vmid }}"
        if qm status "$VMID" >/dev/null 2>&1; then
          echo "Deteniendo y eliminando VM $VMID existente..."
          qm stop "$VMID" --skiplock 1 >/dev/null 2>&1 || true
          qm destroy "$VMID" --purge 1 --destroy-unreferenced-disks 1
        fi
      args: { executable: /bin/bash }
      loop: "{{ vms }}"
      loop_control: { label: "CLEANUP VM {{ item.vmid }}" }

    - name: Clonar desde template (Full Clone)
      delegate_to: "{{ pve_ssh_host }}"
      become: true
      shell: |
        set -euo pipefail
        qm clone {{ template_vmid }} {{ item.vmid }} --name "{{ item.name }}" --full 1 --storage "{{ storage_system }}"
      args: { executable: /bin/bash }
      loop: "{{ vms }}"
      loop_control: { label: "CLONE {{ item.name }}" }

    - name: Configurar Hardware y Cloud-Init (CPU/RAM/Red/IP Fija)
      delegate_to: "{{ pve_ssh_host }}"
      become: true
      shell: |
        set -euo pipefail
        qm set "{{ item.vmid }}" \
          --cores "{{ item.cores }}" \
          --sockets 1 \
          --memory "{{ item.mem }}" \
          --agent 1 \
          --onboot 1 \
          --scsihw virtio-scsi-pci \
          --cpu host \
          --net0 "virtio,bridge={{ net_bridge }}" \
          --ipconfig0 "ip={{ item.ip }},gw={{ vlan_gateway }}" \
          --nameserver "{{ dns_string }}"
      args: { executable: /bin/bash }
      loop: "{{ vms }}"
      loop_control: { label: "CONFIG {{ item.name }}" }

    - name: Redimensionar disco root
      delegate_to: "{{ pve_ssh_host }}"
      become: true
      shell: "qm resize {{ item.vmid }} scsi0 +{{ item.root_gb }}G"
      args: { executable: /bin/bash }
      loop: "{{ vms }}"
      loop_control: { label: "RESIZE {{ item.name }}" }

    - name: Arrancar nuevas VMs consolidadas
      delegate_to: "{{ pve_ssh_host }}"
      become: true
      shell: "qm start {{ item.vmid }}"
      args: { executable: /bin/bash }
      loop: "{{ vms }}"
      loop_control: { label: "START {{ item.name }}" }

    - name: Esperar conexión SSH en la nueva red
      wait_for:
        host: "{{ (item.ip | string).split('/')[0] }}"
        port: 22
        delay: 10
        timeout: "{{ ssh_wait_timeout }}"
      loop: "{{ vms }}"
      loop_control: { label: "WAIT SSH {{ item.name }}" }
